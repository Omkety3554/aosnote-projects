# configure aws provider to establish a secure connection between terraform and awss
provider "aws" {
  region  = 
  profile = 

  default_tags {
    tags = {
      "Automation"  = 
      "Project"     = 
      "Environment" = 
    }
  }
}
